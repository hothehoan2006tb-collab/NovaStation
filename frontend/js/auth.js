
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('form[data-auth]').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault(); let email=f.email.value; const pwd=f.querySelector('input[type="password"]')?.value; const old=JSON.parse(localStorage.getItem('novastation_user')||'{}'); let name=f.name?.value||(old.email===email?old.name:email.split('@')[0]); let fullName=f.fullName?.value||old.fullName||''; let role='Khách hàng'; if(email.toLowerCase()==='novastation@gmail.com'||name.toLowerCase()==='novastationadmin'){if(pwd!=='nova123456'){if(typeof showToast==='function')showToast('Mật khẩu Admin sai!');else alert('Mật khẩu Admin sai!');return;}role='Admin';name='novastationadmin';email='novastation@gmail.com';} localStorage.setItem('novastation_user',JSON.stringify({name,email,fullName,role})); if(typeof showToast==='function')showToast('Thành công!'); setTimeout(()=>location.href='index.html',1000);}));});
